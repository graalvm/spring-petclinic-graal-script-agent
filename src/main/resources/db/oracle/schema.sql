BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE vets (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    first_name VARCHAR2(30),
    last_name VARCHAR2(30)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX vets_last_name ON vets (last_name)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE specialties (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(80)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX specialties_name ON specialties (name)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE vet_specialties (
    vet_id NUMBER(10) NOT NULL,
    specialty_id NUMBER(10) NOT NULL,
    CONSTRAINT fk_vet_specialties_vets FOREIGN KEY (vet_id) REFERENCES vets (id),
    CONSTRAINT fk_vet_specialties_specialties FOREIGN KEY (specialty_id) REFERENCES specialties (id),
    CONSTRAINT uq_vet_specialties UNIQUE (vet_id, specialty_id)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE types (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(80)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX types_name ON types (name)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE owners (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    first_name VARCHAR2(30),
    last_name VARCHAR2(30),
    address VARCHAR2(255),
    city VARCHAR2(80),
    telephone VARCHAR2(20)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX owners_last_name ON owners (last_name)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE pets (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(30),
    birth_date DATE,
    type_id NUMBER(10) NOT NULL,
    owner_id NUMBER(10),
    CONSTRAINT fk_pets_owners FOREIGN KEY (owner_id) REFERENCES owners (id),
    CONSTRAINT fk_pets_types FOREIGN KEY (type_id) REFERENCES types (id)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX pets_name ON pets (name)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX pets_owner_id ON pets (owner_id)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE visits (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    pet_id NUMBER(10),
    visit_date DATE,
    description VARCHAR2(255),
    CONSTRAINT fk_visits_pets FOREIGN KEY (pet_id) REFERENCES pets (id)
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX visits_pet_id ON visits (pet_id)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE saved_owner_query_scripts (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(255) NOT NULL,
    script_json CLOB NOT NULL
  )';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
@@
